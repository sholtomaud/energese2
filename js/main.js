import AppShell from '../src/app-shell/app-shell.js';

// The customElements.define call is in app-shell.js,
// so simply importing AppShell (and thus executing app-shell.js) is enough to register the component.

console.log('App shell loaded and main.js executed.');
